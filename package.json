{
  "name": "mlh-sample-app-backend",
  "version": "1.0.0",
  "engines": {
	"node": "8.11.4"
  },
  "description": "The backend for the MLH-sample-app",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Bryan Den Hollander",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.4",
    "twit": "^2.2.11"
  },
  "scripts": {
	"start": "node index.js",
	"heroku-postbuild": "cd client && npm install && npm run build"
  }
}
